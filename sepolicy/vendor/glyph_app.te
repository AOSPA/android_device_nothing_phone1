# Define Glyph app domain
type glyph_app, domain;
app_domain(glyph_app)

# Allow Glyph to read and write to LED nodes
r_dir_file(glyph_app, sysfs_leds)
allow glyph_app sysfs_leds:file rw_file_perms;

# Allow Glyph to find the activity service
allow glyph_app activity_service:service_manager find;
